name: Current balance simulation
on:
  push:
    branches: [balance-test-current]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  simulate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: node scripts/apply-approved-reconciliation.mjs
      - run: pnpm exec vitest run src/game/current-balance-simulation.test.ts --reporter=verbose
